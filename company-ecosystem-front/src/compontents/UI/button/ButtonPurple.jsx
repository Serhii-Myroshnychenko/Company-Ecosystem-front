import React from 'react';
import s from './ButtonPurple.module.css'

const ButtonPurple = (props) => {
    return (
        <button className={s.purpleButton} {...props}>

        </button>
    );
};

export default ButtonPurple;