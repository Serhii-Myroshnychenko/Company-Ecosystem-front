import React from 'react';
import s from './ButtonPurple.module.css'

const ButtonEn = (props) => {
    return (
        <button className={s.enButton} {...props}>

        </button>
    );
};

export default ButtonEn;