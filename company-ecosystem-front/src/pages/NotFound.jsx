import React from 'react';

const NotFound = () => {
    return (
        <div styles={{textAlign: "center", fontSize: "30px"}}>
            NOT FOUND PAGE
        </div>
    );
};

export default NotFound;